package basecall.teampkg;

import basecall.basepkg.MyClass;

public team class Team1 
{
	public class Role1<S,T> playedBy MissingBaseclass
    {
		callin T callinMethod(S dummy) {
			T val = null;
			if (dummy != null)
				val = base.callinMethod(dummy);
        	return val;
        }
    }
}
